import { randCreditCard } from '../lib/credit-card';

describe('creditCard', () => {
  it('should create', () => {
    // console.log(randCreditCard());
    console.log(randCreditCard());

    expect(1);
  });
});
